// This file is for generating static params for the shift details page.
// It is a separate file to avoid conflicts with the "use client" directive.

export function generateStaticParams() {
  return [{ id: "1" }, { id: "2" }, { id: "3" }, { id: "4" }, { id: "5" }, { id: "6" }]
}
